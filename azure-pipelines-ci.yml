# Azure Host Agent - CI

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'DockerHub'
    repository: 'registry.joachimveulemans.be/azure-host-agent'
    command: 'build'
    Dockerfile: 'Dockerfile'
    tags: 'latest'
